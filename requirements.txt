## Dépendances principales (pipeline)
python-docx==1.1.2
PyMuPDF==1.24.0
PyYAML>=6.0

## Branding DOCX (logos + parsing XML)
pillow>=10.0.0
lxml>=4.9.0

## Backend API (FastAPI + RQ + Redis + SSE + JWT)
-r backend/requirements.txt

## Legacy (interface Streamlit) — conservée si besoin
streamlit==1.38.0

## Training UI - RAG & LLM
llama-index>=0.10.0
llama-index-embeddings-openai>=0.1.0
llama-index-llms-openai>=0.1.0
pandas>=2.0.0
sentence-transformers>=2.2.0

## Extraction emails Outlook (.msg)
extract-msg>=0.48.0
