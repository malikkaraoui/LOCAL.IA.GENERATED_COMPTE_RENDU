"""Sous-package RAG (ingestion de nouvelles sources locales).

Dans ce projet, le RAG est basé sur un payload "extracted" + chunking + retrieval.
L'ingestion audio ci-dessous produit des fichiers texte (et métadonnées) dans un
emplacement que l'extracteur du pipeline sait déjà parcourir.
"""
